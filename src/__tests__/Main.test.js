import React from 'react';
import ReactDOM from 'react-dom';
import { expect, context } from 'chai';
import {mount, render, shallow} from 'enzyme';

import Main from './../Components/Main';

describe('Main Component', () => {
    it('ss', () => {
        expect('h1').to.equal('h1');    
      });
});